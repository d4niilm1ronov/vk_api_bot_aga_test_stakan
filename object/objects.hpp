#if !defined(VKAPI_OBJECTS)
#define VKAPI_OBJECTS


////////////////////////////////////////////////////////////////////////

// Базовый класс для ВСЕХ объектов
struct vkapi::object {

};

////////////////////////////////////////////////////////////////////////

// Объект, описывающий видеозапись
struct vkapi::video {
    private:

    public:

};

////////////////////////////////////////////////////////////////////////

// Объект, описывающий аудиозапись
struct vkapi::audio {
    private:

    public:

};

////////////////////////////////////////////////////////////////////////

// Объект, описывающий документ
struct vkapi::doc {
    private:

    public:

};

////////////////////////////////////////////////////////////////////////

// Объект, описывающий прикрепленную ссылку в сообщении,
    // комментарии или записи на стене
struct vkapi::link {
    private:

    public:

};

////////////////////////////////////////////////////////////////////////

// Объект, описывающий товар
struct vkapi::market {
    private:

    public:

};

////////////////////////////////////////////////////////////////////////

// Объект, описывающий подборку товаров
struct vkapi::market_album {
    private:

    public:

};

////////////////////////////////////////////////////////////////////////

// Объект, описывающий запись пользователя или сообществ
struct vkapi::wall {
    private:

    public:

};

////////////////////////////////////////////////////////////////////////

// Объект, описывающий комментарий к записи
struct vkapi::wall_reply {
    private:

    public:

};

////////////////////////////////////////////////////////////////////////

// Объект, описывающий стикер
struct vkapi::sticker {
    private:

    public:

};

////////////////////////////////////////////////////////////////////////

// Объект, описывающий подарок
struct vkapi::gift {
    private:

    public:

};

////////////////////////////////////////////////////////////////////////

// Объект клавиатуры для ботов
struct vkapi::keyboard {

};

////////////////////////////////////////////////////////////////////////

// Настройки оповещений для диалога
struct vkapi::push_setting {
    private:
        unsigned int sound;
        int disabled_until;

    public:

};

////////////////////////////////////////////////////////////////////////


#endif