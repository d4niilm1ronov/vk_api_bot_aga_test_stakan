#if !defined(DATE_NAMESPACE)
#define DATE_NAMESPACE

typedef unsigned int uint;

namespace time_university {
    extern uint last_number_lesson;

    extern uint get_current_number_lesson();

    extern uint get_current_date();
};

#endif